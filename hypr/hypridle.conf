general {
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

listener {
    timeout = 10
    on-timeout = $HOME/.config/screen/idle.sh && hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}
